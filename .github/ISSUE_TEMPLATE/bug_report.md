---
name: Bug report
about: A `gst` command, an installer, or a module behaves wrong.
title: "[bug] "
labels: bug
assignees: ''
---

## Summary

<!-- One sentence. What did you expect, what happened instead? -->

## Reproduction

The smallest possible recipe to reproduce. Ideally:

1. Module / recipe ID:
2. Inputs file (paste or attach `services.yaml` with secrets redacted):

   ```yaml
   ```

3. Exact `gst` command you ran:

   ```bash
   gst install ... --dry-run
   ```

4. Output (paste full stderr + stdout, redact tokens):

   ```
   ```

## Expected behavior

<!-- What should have happened? -->

## Actual behavior

<!-- What did happen, including any HTTP status codes or stack traces. -->

## Environment

- `gst --version`:
- Python version:
- OS:
- Grafana Cloud stack region (e.g. `prod-us-east-0`):

## Anything else?

<!-- Workarounds, related issues, etc. -->
